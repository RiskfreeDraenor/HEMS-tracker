# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / tooling
.vscode/
.idea/
.claude/
.wrangler/
*.swp
*~

# Personal-context docs — kept local-only since they contain user-specific
# operational info (CF account ID, email, base addresses, etc.). Claude
# Code still reads them from disk for in-session context.
HANDOFF.md
CLAUDE.md
