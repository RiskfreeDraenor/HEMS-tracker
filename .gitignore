# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / tooling
.vscode/
.idea/
.claude/
.wrangler/
*.swp
*~

# Personal-context docs — kept local-only since they contain user-specific
# operational info (CF account ID, email, base addresses, etc.). Claude
# Code still reads them from disk for in-session context.
HANDOFF.md
CLAUDE.md

# Local-only data dumps — not source. airports.sql is the generated
# populate-airports.mjs byproduct (~2.7 MB OurAirports dump); adsbapi.json
# is a downloaded copy of the adsb.lol OpenAPI spec, kept for reference.
airports.sql
adsbapi.json
